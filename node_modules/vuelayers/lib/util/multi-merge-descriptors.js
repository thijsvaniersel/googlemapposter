/**
 * VueLayers
 * Web map Vue components with the power of OpenLayers
 *
 * @package vuelayers
 * @author Vladimir Vershinin <ghettovoice@gmail.com>
 * @version 0.11.24
 * @license MIT
 * @copyright (c) 2017-2020, Vladimir Vershinin <ghettovoice@gmail.com>
 */
import _sliceInstanceProperty from '@babel/runtime-corejs3/core-js-stable/instance/slice';
import _reduceInstanceProperty from '@babel/runtime-corejs3/core-js-stable/instance/reduce';
import _typeof from '@babel/runtime-corejs3/helpers/esm/typeof';
import mergeDescriptors from 'merge-descriptors';

/**
 * @param {...Object} args
 * @returns {Object}
 */

function multiMergeDescriptors() {
  var _context;

  var redefine;

  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }

  if (_typeof(args[args.length - 1]) !== 'object') {
    redefine = args.pop();
  }

  return _reduceInstanceProperty(_context = _sliceInstanceProperty(args).call(args, 1)).call(_context, function (dest, src, i) {
    return mergeDescriptors(dest, src, redefine);
  }, args[0]);
}

export default multiMergeDescriptors;
